%h2.pull-left List of events

%a.pull-right{:href => "/admin/events/new", :style => "margin-top: 20px", :class => "btn btn btn-primary"} 
  Add a new events

%table.table.table-stripped
  %tr
    %th Title
    %th Created at
    %th Updated at
    %th Action

  - events.each do |e|
    %tr
      %td= e.title
      %td= e.created_at
      %td= e.updated_at
      %td
        %a.btn.btn-xs.btn-default{:href => "/admin/events/edit/#{e.id}"} Edit
        %a.btn.btn-xs.btn-danger{:href => "/admin/events/delete/#{e.id}"} Delete

%a.pull-right{:href => "/admin/events/new", :style => "margin-top: 20px", :class => "btn btn btn-primary"} 
  Add a new events
